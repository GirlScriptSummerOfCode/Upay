import NavBar from '../components/NavBar';
import links from '../constants/links';

/**
 * About page
 * @returns {React.ReactElement}
 */
export default () => (
  <main>
    <h3>About Page</h3>
  </main>
);
